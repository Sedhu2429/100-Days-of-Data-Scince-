Try :

Will Not Take Global Values



| Exception           | Description                                   |
| ------------------- | --------------------------------------------- |
| `ValueError`        | Invalid value (e.g., converting "abc" to int) |
| `TypeError`         | Operation on wrong type (e.g., `3 + "a"`)     |
| `ZeroDivisionError` | Dividing by zero                              |
| `FileNotFoundError` | File doesn’t exist                            |
| `KeyError`          | Accessing a missing dictionary key            |
| `IndexError`        | Accessing a missing list index                |
| `ImportError`       | Module not found                              |
| `PermissionError`   | No permission for file or resource            |
| `Exception`         | Base class of all exceptions                  |


Numpy

[]  = single 
[1,3,5 ,[1,4,5,6,]] = two d
[
    [
        [1,2,3,4,5], [3,5,5,6,7]
    ] , 
    [
        [4,5,6,7] , [4,5,6,7]
    ]
    
     ] = three


shape  = 2,3
matrix
count
bool = index, filert
zero = inti
split
slice

Linear → straight lines between points 📈

Pad (Forward Fill) → repeats the last value ➡️

Backfill → uses the next value ⬅️

Polynomial → draws a smooth curved line 🎢



Data  Frame Concat 
final_data = pd.concat([data1,data2],ignore_index=True)


df.loc[row_selection, column_selection]


1️⃣ Predictive Model Imputation	KNN or Regression	Basic learning-based imputation
2️⃣ Multiple Imputation (MICE)	Iterative Imputer	Statistical iterative replacement
3️⃣ GAN-based Imputation (GAIN)	Deep learning generative approach	Synthetic + realistic filling



1) Predictive Model Imputation	KNN or Regression

from sklearn.impute import KNNImputer

imputer = KNNImputer(n_neighbors=5)
data_imputed = imputer.fit_transform(data)

2)Multiple Imputation (MICE)	Iterative Imputer

from sklearn.experimental import enable_iterative_imputer
from sklearn.impute import IterativeImputer

imputer = IterativeImputer(random_state=0)
data_imputed = imputer.fit_transform(data)


3)GAN-based Imputation (GAIN)	Deep learning generative approach

# Pseudocode
X_imputed = GAIN(X_missing, mask)




trainig & testing split 

optimization 